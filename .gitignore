# Dependencies (devDependencies install here; runtime @deepseek-ai/* resolve from the harness)
node_modules/

# macOS
.DS_Store

# Build temp
*.tsbuildinfo

# Lockfile (devDependencies only; runtime deps resolve from the harness)
package-lock.json
